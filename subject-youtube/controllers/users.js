const axios = require("axios");

// this will decide what happens by what someone does
// http calls in react

// handleClick () {
//     axios.get("destination of sign in")
//     .then(response => this.setState({ user: response.data.something })
// }

// render () {}